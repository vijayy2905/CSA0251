#include <stdio.h>
int main()
{
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    if (a > b && a > c) {
        printf("%d is the Greatest number", a);
    }
    else if (b > a && b > c) {
        printf("%d is the Greatest number", b);
    }
    else {
        printf("%d is the Greatest number", c);
    }
    return 0;
}
