#include <stdio.h>
void main()
{
    int a = 0, b = 1, c, n, i = 1, sum = 0;
    scanf("%d", &n);
    while(i <= n)
    {
        c = a + b;
        a = b;
        b = c;
        if(c % 2 != 0)
            sum += c;
        i++;
    }
    printf("Sum = %d\n", sum);
}
